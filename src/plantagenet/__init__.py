"""Plantagenet Harness — a Python rules engine for GMT's *Plantagenet:
Cousins' War* (Levy & Command series).

The harness holds full game state, validates and resolves rules-defined
actions, rolls all dice from a seeded RNG, and exposes a structured
interface for an LLM (or human) to play one or both sides.

Phase 0 (current): project skeleton, static reference data, CLI
structure, test framework. No game logic yet.
"""

__version__ = "0.1.0"
